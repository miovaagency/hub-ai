[phases.setup]
      nixPkgs = ['pnpm', 'bun']

[phases.install]
      cmds = ['NODE_OPTIONS=--max-old-space-size=8192 pnpm install']

[phases.build]
      cmds = ['NODE_OPTIONS=--max-old-space-size=8192 pnpm run build']

[start]
      cmd = 'NODE_OPTIONS=--max-old-space-size=8192 pnpm start'