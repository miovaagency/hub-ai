[phases.setup]
      nixPkgs = ['pnpm', 'bun']

[phases.install]
      cmds = ['pnpm install --max-old-space-size=6144']

[phases.build]
      cmds = ['pnpm run build --max-old-space-size=6144']

[start]
      cmd = 'pnpm start'