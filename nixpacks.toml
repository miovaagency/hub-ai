[phases.setup]
nixPkgs = ['bun', 'nodejs']

[phases.install]
cmds = [
  'corepack enable',
  'pnpm install'
]

[phases.build]  
cmds = ["NODE_OPTIONS='--max-old-space-size=8192' pnpm run build"]

[start]
cmd = 'pnpm start'

[variables]
NODE_ENV = 'production'
DOCKER = 'true'